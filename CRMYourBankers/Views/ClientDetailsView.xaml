<UserControl
    x:Class="CRMYourBankers.Views.ClientDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="200"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <StackPanel           
            Margin="5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Label Content="Imię:" />
            <TextBox Width="200" Text="{Binding FirstNameText}" />
        </StackPanel>
        <StackPanel
            Margin="5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Label Content="Nazwisko:" />
            <TextBox Width="200" Text="{Binding LastNameText}" />
        </StackPanel>
        <StackPanel           
            Margin="5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Label Content="Numer Telefonu:" />
            <TextBox Width="200" Text="{Binding PhoneNumberText}" />
        </StackPanel>
        <StackPanel           
            Margin="5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Label Content="E-mail:" />
            <TextBox Width="200" Text="{Binding EmailText}" />
        </StackPanel>
        <StackPanel           
            Margin="5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Label Content="Pesel:" />
            <TextBox Width="200" Text="{Binding PersonalIdText}" />
        </StackPanel>
        <Button Command="{Binding SaveButtonCommand}" Width="200" Content="Save" />
        <Button Command="{Binding CancelButtonCommand}" Width="200" Content="Cancel" />
    </StackPanel>
</UserControl>
